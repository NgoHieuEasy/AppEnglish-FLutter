import 'package:shared_preferences/shared_preferences.dart';

class CharacterCore{
//   // Lưu điểm số của nhân vật vào Shared Preferences
//   _saveScore(String character, int score) async {
//     SharedPreferences prefs = await SharedPreferences.getInstance();
//     Map<String, int> scores = Map<String, int>.from(prefs.getString('scores').toString() ?? {});
//     scores[character] = score;
//     prefs.setString('scores', scores.toString());
//   }
//
// // Lấy điểm số của nhân vật từ Shared Preferences
//   _getScore(String character) async {
//     SharedPreferences prefs = await SharedPreferences.getInstance();
//     Map<String, int> scores = Map<String, int>.from(prefs.gfvvetString('scores') ?? {});
//     int score = scores[character] ?? 0;
//     return score;
//   }
}